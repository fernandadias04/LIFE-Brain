import 'react-native-gesture-handler';
import React, { Component } from 'react';

import {

  View,
  Text,
  Button,
  Image,
  ImageBackground, 
  ScrollView,
  StyleSheet,
} from 'react-native';

import {Avatar} from 'react-native-elements';

export default class desenvolvedores extends React.Component {
    render(){
        return(
            <ImageBackground>
                <ScrollView>
                    
                </ScrollView>
            </ImageBackground>
        )
    }
}